# Fixtures

This directory will contain only synthetic or explicitly consented test fixtures and
their expected baselines. Fixture manifests and versioned configuration data carry a
`version` key that is echoed in applicable responses.
