# Accessibility checks

ResumeMatch runs automated axe checks and keyboard workflows over the upload and profile-review screens. These checks cover detectable WCAG 2.1 AA issues, accessible names, live announcements, and keyboard activation.

Automated checks are partial. They do not replace manual testing with screen readers, keyboard-only navigation across supported browsers, or expert accessibility review.
