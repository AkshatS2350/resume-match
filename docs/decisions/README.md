# Architecture decisions

This directory records accepted and proposed architecture decisions. Decisions use a
numeric identifier and state their status; versioned behavior described by a decision
is implemented in configuration carrying a `version` key echoed in responses.
