[pytest]
addopts = -ra -m "not live"
markers =
    live: requires external network access
    boundary: enforces a privacy or architecture boundary
    determinism: deterministic scoring and matching reproducibility properties
    perf: measures a published performance budget
