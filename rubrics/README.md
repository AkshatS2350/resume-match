# Rubrics

This directory will contain declarative, versioned role-rubric files. Each file carries
a `version` key that is echoed in readiness responses; rubric additions must remain
schema-conforming configuration, never scoring-code branches.
