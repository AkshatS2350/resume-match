# ResumeMatch

ResumeMatch is a privacy-first, deterministic job-application decision engine.

The project is currently implementing its M0 repository and contract scaffolding. See
the approved specifications in `.kiro/specs/resumematch/` before changing behavior.

## Development

Backend commands run from `backend/` with `uv`. Frontend commands run from `web/` with
`npm`.
